import React from "react";

const TextMove = () => {
  return (
    <div className="min-h-screen relative">
      <h2 className="text-7xl absolute whitespace-nowrap left-[50%] -translate-x-[50%] top-[40%] -translate-y-[50%] rotate-3">
        Frontend - Development - Creating a Web App using React, which is highly
        performance
      </h2>
      <h2 className="text-7xl absolute whitespace-nowrap left-[50%] -translate-x-[50%] top-[53%] -translate-y-[50%] rotate-3 text-gray-400">
        React Developer - React - Using a virtual DOM, React allows the site to
        load with high efficiency
      </h2>
    </div>
  );
};

export default TextMove;
